# Claude Code Instructions

## Memory

Conversation memories are stored in `memory/` at the repo root (gitignored). Check `memory/MEMORY.md` for the index when you need context about the project, user, or past decisions.
